# Backlog

Всё, что известно как стоящее работы, и ничего кроме. Читать перед тем, как
предлагать работу. Записывать сюда, а не упоминать один раз в разговоре.
Закрывать пункт — удалять его.

Это бэклог ядра: сам офис и бесплатные модули. Платные модули, бизнес и
внутренние документы живут в приватной половине, `modules/BACKLOG.md`.

Дописывать в конец раздела, а не в начало: файл правят все, и добавление в
конец превращает большинство конфликтов в их отсутствие.

Пункт говорит что и почему в одну-две строки. Если нужен абзац, это ещё не
пункт бэклога, а вопрос, который надо задать.

## Bugs

- Поток не открывается, если `/api/whoami` упал: `openStream()` только в
  success-ветке `web/main.js`. Пустой офис без объяснения.
- Клавиша `I` (приглашение) недостижима: её раньше перехватывает инвентарь в
  `web/main.js`, ветка приглашения мёртвая.
- `JSON.parse(localStorage)` на верхнем уровне `web/main.js` без `try/catch`:
  одно битое значение убивает загрузку страницы.
- i18n печатает «нет перевода — en:title.day.few» при каждой загрузке: у
  английского нет формы few, самопроверка сравнивает по русским ключам.
- Дефолты модулей не попадают в кэш настроек: `getSettings()` в `index.js`
  вызывается раньше `loadModules()`, кэш не пересобирается.
- `tools/release.mjs` выполняет git в `process.cwd()`, а файлы правит от
  `import.meta.url`: из подкаталога релиз применяется наполовину.
- Интервал печатной машинки не сбрасывается в `closeDialog`, `geoTimer` не
  сбрасывается в `closeSky` (`web/ui.js`).
- `tools/test-consent.mjs` падает через раз в общем прогоне и проходит четыре
  раза подряд поодиночке. Порт у него фиксированный — `CONSENT_PORT`, по
  умолчанию 5393, — и сервер он поднимает свой; похоже на гонку за портом с
  предыдущим стендом, не успевшим его отпустить. Найдено 4 сентября 2026:
  два полных прогона подряд дали 0 и 1 упавший при одном и том же коде.

- `shot.mjs` не переключает вкладки инвентаря цифрой: `--keys "c,3"` и
  `--keys "c,4"` оставляют «на себе», хотя в браузере те же клавиши работают,
  а стенд `test-tree.mjs` их проходит. Найдено 4 сентября 2026 на кадре
  дерева модулей: панель открылась, «4» не дошла. Пока снимать вкладки
  инвентаря можно только живым браузером.

## Next

- Кнопка копирования у блоков кода в транскрипте и в просмотре файла.
  Команды из ответа агента сейчас переносят руками; 4 сентября 2026 это
  случилось прямо в разговоре — две команды `git push`. Кнопка в правом
  верхнем углу блока: в покое тусклая, цвета подписи языка, при наведении
  загорается, после клика полторы секунды говорит «скопировано». Клавиша Y
  копирует верхний блок в экране — офис клавиатурный. Буфер без https есть
  только на localhost, поэтому запасной путь через `execCommand` и честное
  «не вышло» на кнопке, если и он отказал. Нарисовано, ждёт утверждения —
  секция
  [WIP — Кнопка копирования у блоков кода](https://www.figma.com/design/izt4d17qotvyIv7r6BJdSY/AI-Valey?node-id=1005-2).
  Точки: `web/markdown.js`, `web/markdown.css`, `web/ui.js`, `web/i18n.js`.

- Атомарная запись настроек (`.tmp` + `rename`), очередь записей, бэкап
  битого файла вместо тихого сброса в дефолты.
- HTML и SVG из `/api/file` отдавать как attachment или text/plain с nosniff:
  сейчас исполняются на origin офиса, где в localStorage лежит токен.
- Единый `esc()` для `& < > " '` в панелях: имена файлов, проект, ветка,
  метки геокодера и текст ошибок идут в innerHTML без экранирования.
- `es.onerror` с переподключением; переоткрывать поток после `makeInvite`.
- Прокси обложек радио: `redirect: 'error'` и белый список `image/*`.
- CI: workflow с матрицей Node 18/20/22, `node --test` вместо shell-цикла
  (он падает без `modules/`), c8 для покрытия.
- Серверные тесты воспроизводимо: `VALEY_SETTINGS` во временный файл в
  test-presence, переопределяемый `CLAUDE_DIR` с фикстурным транскриптом для
  test-consent, порт 0 вместо фиксированных.
- `createHandler(deps)` из `server/index.js` без `listen` при импорте, чтобы
  тестировать маршруты in-process. Покрыть deliver.js, weather.js,
  radio/server.js, `/api/file`, гейт 404/401 и redirect токена.
- Общий DOM-шим в `tools/lib/` вместо семи копий; убрать тавтологии
  (test-art:65, test-floors:80), `!== 403` в test-guest:93, `scrollTop < 0`.

## Someday

- Таблица маршрутов на сервере вместо цепочки `if` и `config.js` для констант.
- Разрезать `main.js` и `ui.js` по швам; общие `px`, плюрализация, `esc`,
  форматтер даты вместо шести, четырёх, трёх и четырёх копий.
- Offscreen-канвас для статичных полов и стен; кэш градиентов и виньетки.
- Доступность: `role="dialog"`, `aria-label` на кнопках закрытия, `aria-live`
  на тостах, не перехватывать Tab и стрелки внутри `<select>`.
- README: «семь файлов сервера» при десяти, список клавиш без B, I, T, таблица
  переменных окружения, описание `/api/*`, упоминание стенда и `sheet.html`.
- `release.mjs`: проверка ветки, `fileURLToPath`, тест на временном
  репозитории; поставить теги v0.1.0 и v0.2.0 на их коммиты.
- `shot.mjs`: обрабатывать `m.error` в `send()`, свободный CDP-порт, `--help`.
- Чистить кэш транскриптов по исчезнувшим сессиям; SIGKILL после grace-периода
  в deliver; не кэшировать неудачу `findCli`.
- Захардкоженные строки в i18n (`main.js` тосты, названия служебных комнат в
  `layout.js`), enum погоды на `'procedural' | 'real'`.

## Waiting on someone

<!-- Пункты, упёршиеся в человека или внешнюю вещь. Кто или что, и с какого
     числа: запись, пролежавшая здесь месяц, что-то говорит. -->
